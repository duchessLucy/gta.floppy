#define gta "Grand Theft Auto by Rockstar Games"
#define GTA "Toronto Pearson Airport"
#include <ostream>
#include <iostream>
#include <list>
#include <string>
#include <map>
#include <unordered_map>
#include <vector>
#include <ctime>
#include <cstdio>
//#include "N64Controller.h"
#define Controller "any way to control your own unit"
#include <algorithm>
#include <random>
#include <cstdlib>

void Offense();
void DisorderlyGaming(bool notOutright = false);
void LackNeed(bool NeedForSpeed);

class Need
{
  protected:
        Need(Need &&);
        Need();
        bool want;
        bool accountable;
};
void noSecurities(char sexurities);
class Money
{
        char fiscalAgent;
        char financialAdministrator;
        double cash;
        unsigned long securitiesInvestments;
        short securityPayments;
        unsigned int securities;
        double credit;
        char bank;
        double chequings;
        double savings;
        long int property;
        long long investment;
        void noPublicPrivateSafe(char organization);
        static unsigned int safe;
        double safeCollections;
        float gratudity;
        float anninuity;

  protected:
        double Dollars;
        char brackets;
        long long stocks;
        char SpiritOfMoney;
};
class WantedLevel
{
  public:
        bool maxWantedStarsLevelIsSix;
        bool maxHeatStarLevelIs5;
        WantedLevel();

  protected:
        WantedLevel(WantedLevel &&);
        ~WantedLevel();
};
class SharkCard
{
};
void NoGame(bool isGame, bool NoActInEffect, bool fauxOnly);
void insanity(bool doNotFightInsanity, int gameInsanity);
void OffensesInRelatingToOfferingProvidingOrObtainingSexualServicesForConsideration(bool chargeTwoHundredThirteen);
void NoEscape();
std::string escape;
void Dying();
void Injury();
void Mutilate();
void ChildrenAreAdults();
void NoInsurance(bool notInsuredByEmpire, bool notCovereredByRespectiveEmpire);
bool atLeasure;
void NoBabies(bool adult);
void NoPets();
void NoGrounding();
void NoGettingHigh();
void CompletingCircuit();
void noWingdingNames(bool noName);

class NoName
{
  protected:
        char groceryStoreBrand;
};

class Phone
{
        float communications;

  public:
        char PhoneForEmergencyServices;
        bool PhoneCallEmergencyServices;
        struct PhoneEmergencyServices
        {
                bool CallToVindicatedStars;
                bool transferWantedStars;
                bool OneForFireDepartment;
                bool TwoForAmbulance;
                bool ThreeForPolice;
                bool FourForPeaceOfficer;
                bool FiveForMilitary;
                bool SixForOfficials;
                bool SevenForFiends;
                bool EightForEpsolom;
                bool NineForPeacefulPassiveMode(bool equipNPCStrength);
                bool ZeroForHangUp;
        };
        void noConjugalRelationships(bool NotIntimatePartners);
        bool PhoneSpouse;
        bool PhoneSoulMates;
        bool PhoneSoulMate;
        bool PhoneFriend;
        bool PhoneFiend;
        bool PhoneNPC;
        bool PhoneOrganization;
        bool MCClub;
        struct phonePlayer
        {
                bool goodBehaviourPlayer;
                bool PhoneViaAuthority;
                bool PhonePlayers;
                bool PhonePlayer;
                bool PhonePlayerJohn;
        };
        void NoMeetFriend(bool meetFriend, bool meetFiend, char GPSWaypoint, std::string GPSToFriend, std::string GPSToFiend);

  protected:
        Phone();
        Phone(Phone &&);
        void NoService();
        void noSoulMateContacts(bool AThing);
        void wrongVoice(bool atLeisure);
        int contacts;
        class Contacts
        {
                char contacts;
                Contacts();
                Contacts(Contacts &&);
                ~Contacts();
                void noPimpServicesContact(bool noPimpServicesContact);
        };
        int joinJobs;
        int internet;
};
class Bar
{
  public:
        long tavern;
        int food;
        int drink;

  protected:
        short underAge;
        long adult;
};
void NoShoulderWidth(bool width, bool with, bool impractible);
void NoSqeezingInTraffic(long roadWidth);

class ChinaTownWarsGangs
{
        bool cacheBoxIsPublic;
        char lockedAndOrClosedCacheBox;

  protected:
        char fiveFingerCountdlDown;
        unsigned int holdAllUnitSlots = 50;
        void NoStashUnitCapacityInfinite(char drug);
        class Drugs
        {
                char highValue(bool heroin, bool coke);
                char mediumValue(bool excstasy, bool acid);
                char lowValue(bool weed, bool downers);
                char lucrativeValue(bool uppers, bool dateRape);
                void losePersonalUse(bool notThumbSupplyProduceAndSell);
                void NoUsing(int drugs, bool useDrugs);
        };
        void shedding(bool unwantedEnergy);
        void offenseONLINE();
        char CTWGang[32];
        char cacheBox;

  private:
        char dealer;
        int holdAllUnitCount;
        char holdAll;
};
class ShortTitles
{
  protected:
        char Acts;
};
class Alignment
{
  protected:
        void none();
        void evil();
};
class Barracks
{
};
class Parents
{
};
class Race
{
};
class Class
{
  protected:
        Class();
        class Gender
        {
                bool maleBotton;
                int male;
                bool femaleBottom;
                int female;

          public:
                char realmOfLovecraft;
                bool satur;
                bool transvestite;
                bool maleIn;
                bool maleOut;
                bool femaleIn;
                bool femaleOut;
                bool maleTop;
                bool femaleTop;
        };
        class Equipment
        {
          protected:
                void TakeStartingEquipment();
                void noStartingEqiupment();
                long phoneInsurance;
                void noPhone(int phone, bool noPhone);
                bool NotAppendage;
        };
        class Background
        {
        };
        class Occupations
        {
          protected:
                std::string OccupationCard;
                int creditRating;
        };
        class Official
        {
                bool ifHoldsOffice;

          public:
                bool noLeisure;

          protected:
                long lives;
                long insurance;
                short travels;
                bool canNotArrest;
                long carInsurance;
                bool doesNotHoldOffice;
                bool isNotLawEnforcement;
                bool noNPCStrength;
                void noArrestPlayers(bool notCharged, bool alaccusedIsNotAccountable, bool released);
                void funPolice(bool policeAgainstFun);
                bool notOwnerOfBailOffice;
                bool isNotPeaceOfficer;
                bool noPeaceOfficerCommission;
                void noCapture(bool deadOrAlive, bool gotContactInfo);
        };

        void noRockstarGames(bool rockstar);
        void noOfficials(bool noLeisure);
        ;

        class IsFun
        {
                std::string ADHDGamingRhythm;
                void DidNotChooseBattleWisely(bool accountablitity, bool liability);
                float patience;
                bool noGrounding;
                bool noGettingHigh;
                float faux;
                bool completedCircuit;
        };
        void isBoringExperience() { std::cout << " groundWithCurrentChosenOccupation" << std::endl; };

        void falseGender();
        void falseRace();
        float patience;
        void noGenitals(bool notBoy);
        void noGenitailia(bool notGirl);

        class WorldOfGames
        {
                std::string PrototypeGame;
                std::string WorldOfWarcraft;

          public:
                std::string GrandTheftAutoSeries;
                std::string RedDeadRedemption;
                std::string LANoir;
                std::string sports;
                std::string Pathfinder;
                std::string dungeonsAndDragons;
                void noAssasinsCreed(float cleansin);

          protected:
                std::string SaintsRow;
                std::string Sims;
                std::string SimCity;
                std::string HabboHotel;
        };
        class Sims
        {
          protected:
                char soul;
                int Simoleans;
        };
        class MirageOS
        {
                void noNaturalAugmentedReality();

          public:
                bool notGameSpace;
                bool notElvenSunkenRuinsWithWaterEffects;
        };

        bool loveForRockstarGames;
        class RockStarGames
        {
                bool friend RockStarGames();
        };

        class GrandTheftAuto
        {
          protected:
                char body;
                std::string isGame = "is a game and expression used tor fun and effect";
                class multiMaps
                {
                  private:
                        std::string dwellings;
                        std::string dwellingsAtNight;

                  public:
                        std::string metaMaps;

                  protected:
                        std::string GtaZero = "The core edition to Grand Theft Auto";
                        unsigned long long GTAOmega;
                };
        };
        void noLightingSmokeWhileDriving(bool driving, bool isDriving, bool haveSmoke);
        class gtaBaseDotCom
        {
        };
        void noTeleport();
        void noAutoTravel();
        void noFastTravel();
        void noMagick();
        void noMagic();
        void noParanormal();
        void noSpeedometer(int velocityGugage);
#define SlowSpeedRace "safe driving maximizes"
        void noSlowSpeedChase(bool noStreetRacing, bool escape, bool ticket);
        void noJustice();
        void noJusticeOfThePeace();
        void noPeaceOfficerEquiptedForArrests(bool noPolicePeaceOfficerVehicleOrginalVehicleForPeaceOfficers, bool noPeaceOfficerRedAndBlueLightsAndSirens, bool noPeaceOfficerUniformOnOutfitEquipt, void noHandcuffs(float handcuffType));
        void PeaceOfficersCanNotArrest(bool charged, bool aRestMeansArrest);
        void NoHeatLevel(int maxHeatStars = 6);
        void PeaceOfficerWanted(bool wantedStarsAreViaVindication, bool wantedStarsAreViaDuty, bool policeActivelyChasePeaceOfficers, bool doNotRecieveEmergencyLineDeployments, bool policeDoNotRollWithPeaceOfficers, bool PhoneMerryWeather, bool PhoneOfficial);

        void NoTurningSignal(bool leftBlinker, bool rightBlinker, bool hazardLights);

        class RegularSpeedIsFastest
        {
                bool accelerate;

          protected:
                bool braking;
                bool horn;
                bool blinkers;
                void noFun();
                void speedingIsEfficient(bool danger, bool needForSpeed);
        };

        class conveyanceOffense
        {
        };

        class Restitition
        {
          private:
                double amount$$$$$$$$$;
                std::string banking;

          protected:
                void noUlaMula(char ulamula);
                void NotWhole(bool marime);

                std::string transaction;
        };
        class Hospital
        {
          public:
                std::string bypassHospital;

          protected:
                std::string healFull;
                std::string teleportRespawn;
        };
        class Money
        {
                struct money
                {
                        std::string moneySymbol = "$";
                        double change;
                        int cash;
                        int bank;
                        int profit;
                        int stock;
                        int land;
                        int karma;
                        int health;
                        int snacks;
                        int foodAndDrugs;
                        int property;
                };
        };

        class transactions
        {
          protected:
                bool noBank;
        };

        class Taxi
        {
                char taxiCabPhoneContact;
                char WayPoint;
                std::string TaxiesYouToWayPoint;

          public:
                bool hailTaxi;
                long taxiService;
        };
        class Combat
        {
                void noRobbery(bool notVindicatedForOffensesResemelimgTheft);
                bool robbery;
                unsigned int lootCollection;

          public:
                bool nonCulpable;
                ;
                void noTaxes(bool notNoble);
                void taxedToDeath(bool culpable, bool canNotCompleteContractsForKing);
                Combat();
                double DropMoneyLoot;

          protected:
                bool noReticule = 0;
                void noTicketCollection(bool isEnforcementAuthority);
                void noRest(void arrest(bool aRestMeansArrest));
                bool arrest;
                bool ticket;
        };
};
class Weapons
{
        int weapons = 105; //number from july 2025
        Weapons();

  public:
        bool RestockAndSupply;
        std::string PurchaseAmmoAtAmmunation;
        std::string purchaseWeaponsAtAmmunation;
        int bullets;
        int energyCells;
        bool noTickleWardare;

  protected:
        float weaponWheel;
        char ammunationWeaponsAndAmmo;
};
class OwnedProperty
{
  private:
        bool noSeclusion;
        bool noLeisure;
        bool patience;
        bool noShibangs = false;
        bool shortReat;

  public:
        bool longRest;

  protected:
        bool OriginalPerspnalVehicle;
        bool OwnedPropery;
        bool OwnedVehicle;
        std::string propertyActivities;
};
class buildings
{
  public:
        int enterableBuilding;
};
class roundabouts
{
};
class roads
{
  public:
        std::string road;
        std::string avenue;
        std::string street;
        std::string highWay;
        std::string alley;
        std::string parking;
        std::string parkingGarage;
        std::string sidewalks;
        std::string path;
        std::string backRoads;
        std::string offRoadTrack;
        float shoulderWidth;
        std::string offRoadPath;
};
class travel
{
        std::string wayPoint;

  public:
        std::string onFoot;
        std::string driving;
        std::string riding;
        std::string passanger;

  protected:
        void danger();
};
class store
{
  public:
        std::string modShop;
        std::string Ammunation;
        std::string convienienceStore;
        std::string clothingStore;
        std::string tattooParlour;
        std::string CluckAndBell;
        std::string brothel;
        std::string maskStore;

  protected:
        std::string snacksAndAmmo;
        std::string snacks;
};
class cops
{
  public:
        ~cops();
        bool isWasted;
        bool isLawEnforcement = true;
        bool CanArrestAnyone;

  protected:
        cops();
};
int wantedLevel;
class strippers
{
  public:
        std::string strippersClub;

  protected:
        bool combatInStripperClub = false;
        void combatAvailableInStripperClub();
};
void noMaleJiggalos();
void inapporiateSexualAttractions();
void noSex();
void noProstitution();
void noProstitutionByPlayer();

class prostitute
{
        std::string phoneContactPimp = "Pimping Service";

  public:
        bool isProstitute;
        static int twoDigitGenderNumberComprisedOfLifePathNunberThenAttitutudeNumber;
};
int healBothPartiesViaTheirSexualActivity;
int callForHookUp;
bool getPickedUp;
bool pickUpHooker;
std::string parkSomewherePrivate;
std::string selectAffordedService;
bool Secluded;

class PoorProstitute
{
  public:
        struct standardSexualServicesCost
        {
                std::string rub = "Fork";
                std::string fuck = "'Nuff";
                double free = 10;
        };

        class CommomProstitute
        {
          public:
                struct standardSexualServicesCost
                {
                        int handjob = 20;
                        int blowjob = 50;
                        int hipJob = 70;
                };
        };
        class ClassyProstitute
        {
                struct standardSexualSevicesCost
                {
                        int heads = 50;
                        int tails = 70;
                        int Both = 100;
                };
        };
        class prostituteType
        {
                bool highEndCallGirl;
                bool MaleJiggalo;
                bool streetWalker;
        };

  protected:
        std::string pimpingRules = "don't' sleep with anyone that you wouldn't want to spend the rest of your life with and/or have a child with. That applied to all hookers";
        void failPimpingRules();
        std::string GuaranteedPayForService;
        bool prostitutionTerritory;
};

class John
{
  private:
        std::string inviteJohn;
        std::string pickUpJohn;

  public:
        std::string phoneContactPimpingService;
        John();

  protected:
        double money;
        int cash;
};
class Insurance
{
  public:
        std::string MorsMutualInsurance;
};
void noPayingPlayerToPlayer();

class BAWSDAQ
{
        double money;
};
class NPC
{
        bool isDangerous;

  public:
        bool isNonCulpable;
        struct position
        {
                float x;
                float y;
                float z;
        };
        bool isArmedWithWeapon;
        bool isWasted;
        std::string staysPulledOverWithFlashingPoliceCarBehindThem;

  protected:
        int cashLoot;
};
class mechanic
{
        void impoundedVehicle();
        void vehicleDamage();
};
void noSpeedometer(bool speedometer, int speedometerGuage);

class traffic
{
  public:
        std::string drivenVehicles;
        std::string pedestrians;
        std::string vehicles;
        std::string entities;

        std::string changeOfSpeedCanCauseTrafficJamAndCanBeATacticAgainstPursuers;
};
char safeDriving;
bool noAlertness;
char safeAndLegalLimits;
char Need4SpeedSteetLegal;
void boringAtSpeedLimit();
float routeTribunal;

class vehicle
{
        int vehicle = 782; //numbers from july 2025
        int super = 60;
        int motorcycle = 58;
        int planes = 42;
        int helicopters = 26;
        int boats = 20;
        int sports = 112;
        int muscle = 83;
        int offRoad = 73;
        int cycles = 9;
        int commercial = 18;
        int compact = 16;
        int coupes = 19;
        int emergency = 34;
        int industrial = 12;
        int military = 17;
        int openWheel = 4;
        int remoteControl = 2;
        int sedan = 42;
        int service = 15;
        int sportsClassic = 47;
        int SUV = 51;
        int tuner = 20;
        int utility = 20;
        int van = 32;

        std::string personalVehicle;
        int garagedVehicle;

  public:
        int PersonalVehicle;
        bool drivingPersonalVehicle;
        bool drivingVehicle;
        bool drivingStolenVehicle;

  protected:
        int PegasusVehicle;
};

class activities
{
};
class jobs
{
        int adversaryModes;
        int contactMissions;
        int heists;
        int races;
        int businesses;
        int freemodeActivities;
        std::string allOtherTypesOfJobsReleasedAcrossTheManyTitleUpdates;
};
void NoChangeNameWhenRetireOrganization();
class iFruit
{
  protected:
        std::string phone;
};
class Player
{
        Player();
        std::string playerUnit;
        nullptr_t unit;
        int cash;
        int hitPoints;
        int armor;

  public:
        char player;
        static int genderNumber;
        std::string worldOfWarcraftRace;
        short age;
        class ProperGender
        {
                ProperGender(const ProperGender &);
        };
        static bool transvestite;
        ;

        static int Avatar;
        struct position
        {
                float xPosition;
                float yPosition;
                float zPosition;
                bool subterrainiumPosition;

                bool isProstitute;
                bool isCop;
                bool isWasted;
                int cashLessThanOrEqualTo5000DroppedFromWasted;

          protected:
                bool brokenBody;
                void noNudity();
                double money;
                int ownedProperty;
                int personalVehicles;
                long sept;

                int rp;
                int jp;
        };
};

void wrongGenderVoice();
class correctVoice
{
        static double dripNig; //code provided bg spirit
        static int dropNig;
        static float whoaoNi;
};

class spouse
{
  protected:
        friend bool spouse(std::string sideKick);
};

// Structure to hold bounty target info
struct BountyTarget
{
        std::string name;
        std::string type; // Standard, Most Wanted, etc.
        int reward;
        std::string missionStage;
};

class Pet
{
  private:
        std::string name;

  public:
        std::string dungeonsAndDragonsMiscellaneousCreature;

  protected:
        Pet();
        Pet(Pet &&);
        bool noNPCPower = false;
        bool noNPCStrength = false;
        bool noNPCImmortality = false;
};
void wrongVoice();
void wrongVoice(int voiceBox);

// Function to display target info
void displayTarget(const BountyTarget &target)
{
        std::cout << "🔹 Name: " << target.name << "\n"
                          << "   Type: " << target.type << "\n"
                          << "   Bounty: $" << target.reward << "\n"
                          << "   Mission Stage: " << target.missionStage << "\n\n";
}

int getDailyIndex(int totalTargets)
{
        std::time_t now = std::time(nullptr);
        std::tm *localTime = std::localtime(&now);
        int dayOfYear = localTime->tm_yday; // 0–365
        return dayOfYear % totalTargets;
}

int main(int argc, char *argv[])
{
        std::cout << "🍎iFruit\n"
                          << std::endl
                          << "GTA: Grand Theft Auto\n⭐️ By Rockstar Games ⭐️⭐️⭐️⭐️⭐️" << std::endl
                          << "\nReady Player One!" << std::endl;
                void NoPlayerUnit();
                void noStartingVehicle();

        void Bills(bool noBillsEver, bool billions, bool profitInstead, float profit);
      //  void Jealousy();
  void noPersonalizedUnits();
        void NoPlayerUnit();

        std::map<std::string, int> const map, Local;
        std::map<std::string, int> const Babylon;
        std::map<std::string, int> const Toronto; //for Canada
        std::map<std::string, int> const Leonida;
        std::map<std::string, int> const LosSantos;
        std::map<std::string, int> const LibertyCity;
        std::map<std::string, int> const DowntownDistrict;
        std::map<std::string, int> const ResidentialDistrict;
        std::map<std::string, int> const IndustrialDistrict;
        std::map<std::string, int> const ViceCity;
        std::map<std::string, int> const regional;
        std::map<std::string, int> const worldly;
        std::map<std::string, int> const Multiversal;
/*
        std::random_device countMoney;
        std::mt19937 generator(countMoney());

        std::uniform_int_distribution<double> change(3.51, 9.99);
        std::uniform_int_distribution<unsigned int> cash(1, 99999999);

        std::uniform_int_distribution<unsigned long long> bank(1, 99999999999999999);
        std::uniform_int_distribution<double> profit(1.00, 9999);
        std::uniform_int_distribution<unsigned long long> BAWSAQ(0.01, 99999999999999999);
        //std::uniform_int_distribution<unsigned short> thirtyFifthMoneyDigit(1, 999);
        std::uniform_int_distribution<double> stockChangePercent(1.11, 99.99);

        double money_change = change(generator);
        unsigned int money_cash = cash(generator);
        unsigned long long money_bank = bank(generator);
        double money_profit = profit(generator);
        unsigned long long money_stock = BAWSAQ(generator);
        double money_stock_change = stockChangePercent(generator);

        //      unsigned short money_highest_digit = thirtyFifthMoneyDigit(generator);
        //      unsigned long long multiverssl_amount_after_seventeen_digits = bank(generator);

        std::cout << "💵 Maze Bank 💵"
                          << std::endl
                          << "Your change amount is $" << money_change << std::endl

                          << "Your bank amount is $" << money_bank << std::endl
                          << "Your profit is $" << money_profit << "×10^6" << std::endl

                          << "Your stock amount is $" << money_stock
                          << std::endl
                          << "Your cash amount is $" << money_cash << std::endl
                          << "Your stock change is %" << money_stock_change << std::endl
                          << std::endl;
*/
        // Full list of standard bounty targets
        std::vector<BountyTarget> allTargets = {
                {"Tommy Lim", "Standard", 35000, "Last seen near Downtown Vinewood"},
                {"Ricky Ji", "Standard", 36500, "Spotted leaving Little Seoul arcade"},
                {"LJ Ha", "Standard", 37000, "Tracked to a warehouse in Cypress Flats"},
                {"Serenity Pierce", "Standard", 35500, "Lost MC hangout in Blaine County"},
                {"India Wood", "Standard", 36000, "ATM scam traced to Mirror Park"},
                {"Hunter Duggan", "Standard", 38500, "Sighted at Diamond Casino rooftop"},
                {"Brigitte Foster", "Standard", 34000, "Checked into a resort in Chumash"},
                {"Lil Prince", "Standard", 34500, "Seen near Vespucci basketball courts"},
                {"Cook Kenzie", "Standard", 35000, "Escaped from prison transport on Route 68"},
                {"Angel Kenney", "Standard", 37500, "Evaded arrest in Sandy Shores motel"}};

        // Shuffle and select 3
        std::random_device rd;
        std::mt19937 g(rd());
        std::shuffle(allTargets.begin(), allTargets.end(), g);

        // Display selected targets
        std::cout << "🎯 Today's Active Bounties from Jenette Eclesses:\n\n";
        for (int i = 0; i < 3; ++i)
        {
                displayTarget(allTargets[i]);
        }
        /*BountyTarget mostWanted = {
    "Omar Garcia",
    "Most Wanted",
    125000,
    "Hack Lifeinvader servers → → Capture"
};*/

        std::vector<BountyTarget> mostWantedTargets = {
                {"Grace Whitney", "Most Wanted", 129000, "Morgue infiltration → Rooftop standoff → Capture"},
                {"Cleo Song", "Most Wanted", 127000, "Break into Vinewood mansion → Recover movie prop → Apprehend"},
                {"Brock Thompson", "Most Wanted", 124000, "Track steroid ring → Fight gym guards → Subdue"},
                {"Chaz Lieberman", "Most Wanted", 126000, "Hack poker servers → Raid penthouse → Capture"},
                {"Leroy O'Neil", "Most Wanted", 123000, "Surveil Grapeseed farm → Disable convoy → Arrest"},
                {"Omar Garcia", "Most Wanted", 125000, "Hack Lifeinvader →Infiltrate LS Car Meet → Disable vehicle / Drift race sabotage → Capture"}};

        int index = getDailyIndex(mostWantedTargets.size());
        BountyTarget todayMW = mostWantedTargets[index];

        std::cout << "🚨 Most Wanted Bounty of the Day:\n";
        displayTarget(todayMW);

        /*std::cout << "🚨 Most Wanted Bounty:\n";
displayTarget(todayMW);
*/
        //      displayTarget(todayMW);
        //displayTimeUntilMidnight();

        // Seed the random number generator
        std::srand(static_cast<unsigned int>(std::time(nullptr)));

        // Possible answers
        const char *answers[] = {
                "Animal Materials.",
                "Art & Antiques.",
                "Bullion.",
                "Counterfeit Goods.",
                "Gemstones.",
                "Jewlery.",
                "Medical Supplies.",
                "Narcotics.",
                "Tobacco & Alcohol.",
                "Weapons & Ammo "};

        // Generate a random index
        int deterministicCratesIndex = std::rand() % sizeof(answers) / sizeof(answers[0]);

        // Print the answer
        std::cout << "The buy Crates Type is: " << answers[deterministicCratesIndex] << std::endl
                          << std::endl
                          << std::endl;

        // Seed the  number generator
        std::srand(static_cast<unsigned int>(std::time(nullptr)));

        // Possible answers
        const char *wanted[] = {" ", "⭐️", "⭐️⭐️", "⭐️⭐️⭐️", "⭐️⭐️⭐️⭐️", "⭐️⭐️⭐️⭐️⭐️"};

        const char *heat[] = {" ", "⭐️", "⭐️⭐️", "⭐️⭐️⭐️", "⭐️⭐️⭐️⭐️", "⭐️⭐️⭐️⭐️⭐️", "⭐️⭐️⭐️⭐️⭐️⭐️"};

        // Calculate the number of elements in the array
        int wantedLevel = sizeof(wanted) / sizeof(wanted[0]);

        // Generate a random index
        int wantedIndex = std::rand() % wantedLevel;

        // Print the answer
        std::cout << "Wanted Level:\n"
                          << wanted[wantedIndex] << std::endl;

        int heatLevel = sizeof(heat) / sizeof(heat[0]);

        // Generate a random index
        int heatIndex = std::rand() % heatLevel;

        // Print the answer
        std::cout << "Heat Level:\n"
                          << heat[heatIndex] << std::endl;
  
        std::cout <<"  --- Player Aputure ---\n\n"
        "           *****           \n"
        "        ***********        \n"
        "      ***************                       @#$%^\n"
        "     *****************                      #$%^&\n"
        "    ****    ***    ****                     $%^&*\n"
        "   ****    * * *    ****                    %^&*(\n"
        "   ****    * * *    ****                    ^&*()\n"
        "    ****    ***    ****    \n"
        "     *****************     \n"
        "      ***************      \n"
        "        ***********        \n"
        "           *****           \n";
        std::cout << std::endl
                          << "🟢 System Running!! 🟢" << std::endl;

        return 0;
}
